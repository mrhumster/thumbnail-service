# TODO

## Совершенствование статуса задач

Текущее состояние: прогресс задачи обновляется через gRPC `UpdateStreamProcessing`
только на 2 события — успех (`handler.go:79`, async `asynq.go:32`) и ошибка.
Промежуточного прогресса нет.

Идея (отложено): хранить состояние/прогресс задач в Redis рядом с Asynq-задачей
(отдельные ключи `progress:{id}` + Redis Pub/Sub-канал как сигнал "объект изменился"),
вместо дёргания gRPC/БД на каждом шаге. Фронт получает через WSS сигнал об изменении
и по id ревалидирует кеш (redux). Масштабировать и на transcoder-воркер.

Почему отложено: Asynq `TaskInfo` кастомные поля не поддерживает (движок сам
перезаписывает хеш задачи), нужен отдельный ключ; WS-bridge и фронт — вне этого
репо; требуется общий контракт прогресса с transcoder. Пока оставили текущий
gRPC-подход.
